name: Branch Deleted

on: delete

jobs:
  delete:
    # if: github.event.ref == 'testing'
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        run: echo "Clean up for branch ${{ github.event.ref }}"
      - name: Comparison
        run: echo "(branch && branch.name == 'testing') is ${{ github.event.ref_type == 'branch' && github.event.ref = 'testing' }}"
